{
  "name": "vhs-studio",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && wrangler pages deploy ./dist --project-name=vhs-studio --branch=main"
  },
  "dependencies": {
    "astro": "^6.2.1",
    "jspdf": "^4.2.1"
  },
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
